{% extends 'base.html.twig' %}

{% block title %}Add cast{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="text-center">Add crew member</h1>
            {{ form_start(personForm, {attr: {novalidate: 'novalidate'}}) }}
            {{ form_widget(personForm) }}
            {{ form_end(personForm) }}
        </div>
    </div>
{% endblock %}
