{% extends 'base.html.twig' %}

{% block title %}Add existing cast member{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="text-center">Add existing crew member</h1>
            {{ form_start(existingPersonForm, {attr: {novalidate: 'novalidate'}}) }}
            {{ form_widget(existingPersonForm) }}
            {{ form_end(existingPersonForm) }}
        </div>
    </div>
{% endblock %}
